export default function NotFound() {
    return (
        <main className="not-found">
            {/* This not-found is specific to the meals component only */}
            <h1>Meal Not Found</h1>
            <p>Unfortunately, we could not find the requested page or meal data.</p>
        </main>
    );
}